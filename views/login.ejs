<!DOCTYPE html>
<html>

<head>
  <title>Login</title>
  <link rel='stylesheet' href='/stylesheets/style.css'>
  <link rel='stylesheet' href='/stylesheets/tailwind.css'>
  <link href='https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&family=Montserrat:wght@600;700&display=swap' rel='stylesheet'>
</head>

<body class='pad-50 bg-light font-sans text-dark min-h-screen'>
  <a href='/' class='text-primary text-md hover:underline'>Return to homepage</a>
  <div class='container mx-auto mt-16 max-w-md p-6 bg-white rounded-lg shadow-md'>
    <h1 class='text-2xl font-semibold mb-6'>Sign In</h1>
    <form method='post' action='/login'>
      <div class='form-group mb-4'>
        <label for='emailOrUsername' class='block mb-2'>Email or username:</label>
        <input type='text' id='emailOrUsername' value='<%= typeof emailOrUsername !== 'undefined' ? emailOrUsername : '' %>'  name='emailOrUsername' class='w-full px-3 py-2 border border-gray-300 rounded-lg' required>
      </div>
      <div class='form-group mb-4'>
        <label for='password' class='block mb-2'>Password:</label>
        <input type='password' id='password' name='password' class='w-full px-3 py-2 border border-gray-300 rounded-lg' required>
      </div>
      <% if(errors){ %>
      <ul class='mb-4'>
        <% for(let i=0; i< errors.length; i++) { %>
        <li class='text-red-500'>
          - <%= errors[i].msg %>
        </li>
        <% } %>
      </ul>
      <% } %>
      <button type='submit' class='w-full py-2 px-3 bg-secondary text-white font-semibold rounded-lg'>Sign in</button>
    </form>
  </div>
</body>

</html>