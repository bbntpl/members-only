<!DOCTYPE html>
<html lang='en'>

<head>
  <meta charset='UTF-8'>
  <meta name='viewport' content='width=device-width, initial-scale=1.0'>
  <title>TIC Clubhouse - Update you info</title>
  <link rel='stylesheet' href='/stylesheets/style.css' />
  <link rel='stylesheet' href='/stylesheets/tailwind.css' />
  <link href='https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&family=Montserrat:wght@600;700&display=swap' rel='stylesheet'>
</head>

<body class='bg-light text-dark font-sans min-h-screen'>
  <% include ./partials/navbar.ejs %>
  <div class='flex'>
    <% include ./partials/members-side.ejs %>
    <main class='flex-grow p-4'>
      <section class='bg-white shadow rounded-lg p-6'>
        <h1 class='text-2xl font-semibold mb-6'>Edit User</h1>
        <form action='/member/<%= currentUser.id %>/update' method='POST'>
					<div class='mb-4'>
						<label for='firstName' class='font-bold block '>First Name</label>
						<input id='firstName' name='firstName' class='w-full px-4 py-2 text-gray-700 bg-light border rounded-lg focus:outline-none focus:ring-2 focus:ring-accent' value='<%= typeof currentUser.firstName === "undefined" ? "" : currentUser.firstName %>' />
					</div>
					<div class='mb-4'>
						<label for='lastName' class='font-bold block'>Last Name</label>
						<input id='lastName' name='lastName' class='w-full px-4 py-2 text-gray-700 bg-light border rounded-lg focus:outline-none focus:ring-2 focus:ring-accent' value='<%= typeof currentUser.firstName === "undefined" ? "" : currentUser.lastName %>' />
					</div>
					<div class='mb-4'>
						<label for='aboutMe' class='font-bold block'>About Me</label>
						<textarea id='aboutMe' name='aboutMe' placeholder='Enter about yourself' required class='w-full px-4 py-2 text-gray-700 bg-light border rounded-lg focus:outline-none focus:ring-2 focus:ring-accent' rows='4'><%= typeof currentUser.firstName === "undefined" ? "" : currentUser.aboutMe %></textarea>
					</div>
          <button type='submit' class='bg-blue-500 text-white px-4 py-2 rounded'>Save Info Changes</button>
          <a href='/member/<%= currentUser.id %>' class='text-blue-500 ml-4'>Cancel</a>
        </form>
      </section>
    </main>
  </div>
</body>

</html>